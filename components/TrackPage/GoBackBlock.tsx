import { Box, Button, Grid } from "@mui/material";

const GoBackBlock: React.FC = () => {
  return (
    <Grid>
      <Button>
        Go back
      </Button>
    </Grid>
  )
}

export default GoBackBlock;
